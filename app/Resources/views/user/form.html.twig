{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xs-12">
                <div class="auth-wrapper">
                    <h3 class="text-center mb-3">Vartotojo forma</h3>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <div class="text-center">
                        <input class="btn btn-primary mt-3" type="submit" value="Patvirtinti" />
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}



